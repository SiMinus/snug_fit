export default class Physics {
  constructor() {
  }

  applyGravity(shape, deltaTime) {
  }

  resolveStacking(shape, shapes) {
  }

  update(shapes, deltaTime) {
  }

  isSettled(shape) {
  }

  reset() {
  }
}

